///@desc SlideTransition(mode,targetRoom)
///@arg mode sets transition mode
///@arg targetRoom sets target room when using goto transition

with(obj_transition){

	mode = argument[0];
	if(argument_count > 1){
		target = argument[1];	
	}

}